# 环境变量示例，复制为 .env 并根据需要修改

# OpenAI 相关，可选
OPENAI_API_KEY=
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini

# Embedding 模型（默认使用 Sentence-Transformers）
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# 索引路径（相对当前项目目录）
INDEX_DIR=data/index
RAW_DIR=data/raw
PROCESSED_DIR=data/processed

# 文本切分参数
CHUNK_SIZE=800
CHUNK_OVERLAP=120